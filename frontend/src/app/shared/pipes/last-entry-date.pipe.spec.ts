import { LastEntryDatePipe } from './last-entry-date.pipe';

describe('LastEntryDatePipe', () => {
  it('create an instance', () => {
    const pipe = new LastEntryDatePipe();
    expect(pipe).toBeTruthy();
  });
});
