import { SpinnerColorDirective } from './spinner-color.directive';

describe('SpinnerColorDirective', () => {
  it('should create an instance', () => {
    const directive = new SpinnerColorDirective();
    expect(directive).toBeTruthy();
  });
});
