import { ConfirmEqualityValidatorDirective } from './confirm-equality-validator.directive';

describe('ConfirmEqualityValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmEqualityValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
